const mongoose = require("mongoose");

const fillerSchema = mongoose.Schema({
    filler: String,
    extra: {
        type: mongoose.Schema.Types.ObjectId,
        ref: "Population"
    }
})

module.exports = mongoose.model("Filler", fillerSchema)